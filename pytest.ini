[pytest]
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: интеграционные тесты с PostgreSQL/RabbitMQ через testcontainers (нужен Docker)
